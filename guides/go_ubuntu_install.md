# Go Install Guide

1. ```wget https://go.dev/dl/go1.27.0.linux-amd64.tar.gz```

2. ```sudo tar -C /usr/local -xzf go1.27.0.linux-amd64.tar.gz```

3. ```export PATH=$PATH:/usr/local/go/bin```

4. ```export PATH="$PATH:$(go env GOPATH)/bin"```

